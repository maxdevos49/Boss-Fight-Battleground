using System;
using System.Collections.Generic;
using System.Text;

using BFB.Engine.TileMap;
using Microsoft.EntityFrameworkCore;
using Xunit;
using Moq;
using Moq.Protected;
using Moq.EntityFrameworkCore;

namespace BFB.Test.Engine.TileMap
{
    public class TileMap
    {
        [Fact]
        public void AddBlockTest()
        {
            
//            //Create mock context
//            Mock<TileMapManager> mockInfo = new Mock<TileMapManager>();
//            mockInfo.Object.setBlock(33, 33, 3);
//            
//            //mock setup setting a block value
//            mockInfo.Setup(foo => foo.setBlock(33,33,3));
//            //mock setup getting a block value
//            mockInfo.Setup(foo => foo.getBlock(33, 33)).Returns(3);
//            
//            //mock verifying methods were only used once
//            mockInfo.Verify((foo => foo.setBlock(33, 33,3)), Times.Once());
//            mockInfo.Verify((foo => foo.getBlock(33,33)), Times.Once());
//            
//            //verification for mock
//            Mock.Verify();
        }
    }
}
